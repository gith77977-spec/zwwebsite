# 🚀 ZONEWEAR - Getting Started Guide

## البدء السريع

هذا دليل شامل لتشغيل موقع ZONEWEAR كاملاً (Frontend + Backend).

---

## المتطلبات 📋

### المتطلبات الأساسية:
- **Node.js** v14+ ([تحميل](https://nodejs.org))
- **npm** (يأتي مع Node.js)
- **متصفح حديث** (Chrome, Firefox, Safari, Edge)

### اختياري:
- **Git** لنسخ المشروع
- **VS Code** أو أي محرر نصوص

---

## خطوات التثبيت والتشغيل ⚙️

### الخطوة 1: انسخ المشروع

```bash
# إذا كنت تستخدم Git
git clone <repo-url>
cd zwwebsite

# أو انسخ المجلد مباشرة
```

---

### الخطوة 2: الخادم الخلفي (Backend)

```bash
# الدخول لمجلد الخادم
cd backend

# تثبيت المكتبات
npm install

# تشغيل الخادم
npm start
```

**يجب أن ترى:**
```
╔════════════════════════════════════════╗
║    ZONEWEAR Backend Server Running     ║
║           Port: 3000                   ║
║    Database: SQLite (zonewear.db)      ║
╚════════════════════════════════════════╝
```

---

### الخطوة 3: الخادم الأمامي (Frontend)

افتح نافذة Terminal جديدة:

```bash
# تأكد أنك في المجلد الرئيسي
cd ..  # أو zwwebsite

# تشغيل خادم ويب بسيط (Python)
python -m http.server 8000

# أو إذا كنت على Windows:
python -m http.server 8000
```

**يجب أن ترى:**
```
Serving HTTP on 0.0.0.0 port 8000 ...
```

---

### الخطوة 4: افتح الموقع

افتح المتصفح:
```
http://localhost:8000
```

---

## أماكن الملفات المهمة 📁

```
zwwebsite/
├── index.html              # الصفحة الرئيسية
├── products.html           # صفحة المنتجات
├── about.html              # صفحة عن الشركة
├── contact.html            # صفحة الاتصال
├── admin.html              # لوحة التحكم
├── admin-login.html        # صفحة تسجيل الدخول
├── admin-settings.html     # إعدادات المسؤول
├── style.css               # أنماط CSS
├── script.js               # البرنامج الرئيسي
│
├── backend/                # الخادم الخلفي
│   ├── server.js          # ملف الخادم الرئيسي
│   ├── database.js        # إدارة قاعدة البيانات
│   ├── package.json       # المكتبات المطلوبة
│   ├── .env               # متغيرات البيئة
│   ├── zonewear.db        # قاعدة البيانات (يتم إنشاؤها تلقائياً)
│   └── backups/           # النسخ الاحتياطية
│
├── images/                # صور المنتجات
├── videos/                # الفيديوهات
└── db/                    # ملفات قاعدة البيانات (قديم)
```

---

## المسؤول (Admin) 🔐

### تسجيل الدخول:

```
الرابط: http://localhost:8000/admin-login.html

اسم المستخدم: zonewear2026
كلمة المرور: Wz2L9MqswZweb
```

### لوحة التحكم:
```
http://localhost:8000/admin.html
```

### إعدادات المسؤول:
```
http://localhost:8000/admin-settings.html
```

---

## الميزات الرئيسية ✨

### 1. الواجهة الأمامية (Frontend)
- ✅ عرض جميل للمنتجات
- ✅ عربة تسوق
- ✅ اللغة العربية والإنجليزية
- ✅ تصميم مستجيب للهاتف

### 2. الخادم الخلفي (Backend)
- ✅ نظام مصادقة آمن
- ✅ قاعدة بيانات SQLite
- ✅ API RESTful
- ✅ نسخ احتياطية تلقائية

### 3. لوحة التحكم (Admin Panel)
- ✅ إدارة المنتجات
- ✅ إدارة الطلبات
- ✅ التحليلات
- ✅ إعدادات المسؤول

---

## الطلبات والبيانات 📦

### إنشاء طلب:
1. اختر منتج
2. اختر المقاس
3. أضف للسلة
4. اضغط "طلب الآن"
5. أدخل معلومات التسليم
6. أتمم الطلب

### عرض الطلبات:
```
الذهاب إلى: Admin Panel → Orders
```

---

## النسخ الاحتياطية 💾

### إنشاء نسخة احتياطية:
1. ادخل Admin Settings
2. اضغط "Create Backup Now"
3. سيتم حفظ النسخة بشكل آمن

### مواقع النسخ:
```
backend/backups/backup-YYYY-MM-DD...db
```

---

## حل المشاكل الشائعة 🛠️

### المشكلة: الخادم لا يبدأ
```bash
# تحقق من نصب المكتبات
cd backend
npm install

# تحقق من Port 3000
netstat -ano | findstr :3000
```

### المشكلة: الموقع لا يحمل
```
تأكد أن:
1. الخادم الخلفي يعمل (Port 3000)
2. خادم الويب يعمل (Port 8000)
3. لا توجد مشاكل في الكابلات أو الإنترنت
```

### المشكلة: لا يمكن تسجيل الدخول
```
تأكد من:
1. اسم المستخدم: zonewear2026
2. كلمة المرور: Wz2L9MqswZweb
3. الخادم يعمل (localhost:3000)
```

### المشكلة: الصور لا تظهر
```
تحقق من:
1. وجود ملفات الصور في: images/
2. الأسماء صحيحة
3. إعادة تشغيل الخادم
```

---

## الاتصالات API 🔌

### اختبر الاتصال:
```
http://localhost:3000/api/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "timestamp": "2026-02-22T..."
}
```

---

## الأمان 🔒

### متطلبات الإنتاج:

```javascript
// أنشئ JWT Secret قوي
JWT_SECRET = "استخدم كلمة مرور قوية جداً"

// غيّر كلمة مرور المسؤول
username: "your-username"
password: "strong-password-here"

// استخدم HTTPS
// فعّل CORS بشكل صحيح
// استخدم متغيرات البيئة
```

---

## نصائح مفيدة 💡

1. **لتثبيت البيانات الافتراضية:**
   ```bash
   cd backend
   npm run init
   ```

2. **لرؤية سجل قاعدة البيانات:**
   ```bash
   sqlite3 zonewear.db
   ```

3. **لإعادة تعيين كلمة المرور:**
   احذف البيانات من settingsPage ثم أعد التثبيت

4. **للتطوير بسرعة أكبر:**
   ```bash
   npm run dev  # مع auto-reload
   ```

---

## الهيكل المثالي للتطوير 🏗️

```
Terminal 1 (Backend):
$ cd backend
$ npm start

Terminal 2 (Frontend):
$ python -m http.server 8000

Browser:
http://localhost:8000
```

---

## التطوير المستقبلي 🚀

تم إضافة:
- ✅ خادم Node.js/Express
- ✅ قاعدة بيانات SQLite
- ✅ نظام JWT للمصادقة
- ✅ تشفير bcryptjs
- ✅ نسخ احتياطية
- ✅ إدارة المسؤول

المراحل القادمة:
- [ ] بوابة دفع (Stripe/PayPal)
- [ ] نظام البريد الإلكتروني
- [ ] نظام التتبع
- [ ] تطبيق الهاتف
- [ ] لوحة تحليلات متقدمة

---

## الدعم والمساعدة 📞

للمزيد من المساعدة:

```
📧 البريد: admin@zonewear.com
📱 الهاتف: +213 XXX XXX XXX
🌐 الموقع: www.zonewear.com
```

---

## الملاحظات القانونية ⚖️

- جميع الحقوق محفوظة © 2026 ZONEWEAR
- هذا المشروع مرخص تحت MIT License
- عند استخدام في الإنتاج، تأكد من الامتثال لقوانين حماية البيانات

---

**آخر تحديث:** 22 فبراير 2026

**نسخة:** 2.0 (مع Backend و Database)

---

## قائمة التحقق النهائية ✅

قبل التسليم للعميل:

- [ ] تشغيل الويب الأمامي بدون أخطاء
- [ ] تشغيل الخادم الخلفي بدون أخطاء
- [ ] تسجيل الدخول يعمل
- [ ] المنتجات تظهر بشكل صحيح
- [ ] عربة التسوق تعمل
- [ ] الطلبات تُحفظ
- [ ] لوحة التحكم تعمل
- [ ] النسخ الاحتياطية تعمل
- [ ] الكلمات العربية صحيحة
- [ ] الموقع جميل على الهاتف

---

**استمتع باستخدام ZONEWEAR! 🎉**
